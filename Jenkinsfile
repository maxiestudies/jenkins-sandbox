/* groovylint-disable NestedBlockDepth */
@Library('jenkins-lib@master') _ // groovylint-disable-line

pipeline {
    agent any

    stages {
        stage('Test'){
            steps{
               sh 'echo hello'
            }
        }
    }
}
